#include<stdio.h>
#include<string.h>
int main()
{
	int n;
	scanf("%d\n",&n);
	while(n--)
	{
		char s[210];
		gets(s);
		int dem=1;
		for(int i=0;i<strlen(s);i++)
		{
			if(s[i]==' '&&s[i+1]!=' ' && s[i+1]!='\0')
				dem++;
		}	
		printf("%d",dem);
		printf("\n");
	}
}
